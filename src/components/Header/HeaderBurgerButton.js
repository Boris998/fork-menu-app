import hamburgerMenu from '.././../assets/icons/hamburger-menu.png'


const HeaderBurgerButton = (props) => {
    return (
        <>
            <img src={hamburgerMenu} style={{height: '80px'}}/>
        </>
    );
}

export default HeaderBurgerButton;
